apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nextcloud-admin-secret
  namespace: workload
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-cluster-store
  target:
    name: nextcloud-admin-secret
    creationPolicy: Owner
    template:
      data:
        username: virgil
  data:
    - secretKey: password
      remoteRef:
        key: "op://5vr4hef2746tpplvjx424xafvu/nextcloud-main/nextcloud-admin"
