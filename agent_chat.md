# Agent Chat Notes
Date: 2026-03-05

## Nextcloud Brute-force IP Investigation

Question investigated:
- Nextcloud warning: remote address `23.93.227.242` was brute-force throttled.
- Concern: unknown attacker vs local device.

Findings:
1. The IP `23.93.227.242` is the public egress IP of `nextcloud-vm` (`192.168.6.183`).
2. The throttled traffic was generated by AppAPI/Flow checks, not a browser session.
3. Evidence from Nextcloud logs:
   - `app=app_api`
   - `url=/ocs/v1.php/apps/app_api/ex-app/state?format=json`
   - `userAgent=ExApp/flow/1.3.1 (httpx/0.28.1)`
   - repeated about every 5 minutes
4. Evidence from nginx access log on `nextcloud-vm`:
   - source appears as `127.0.0.1`
   - same AppAPI state URL
   - response `401`
   - same Flow ExApp user-agent
5. Related Nextcloud background job exists:
   - `OCA\AppAPI\BackgroundJob\ExAppInitStatusCheckJob`
6. `app_api:daemon:list` shows daemon `docker_local_vm` registered for `https://cloud.virgil.info`.

Conclusion:
- This is not your phone and not normal Vivaldi traffic.
- The throttle warning was triggered by local AppAPI/Flow probe behavior (self-originated path), not random internet login attempts.

Action taken:
- brute-force counter for `23.93.227.242` was reset.

Suggested next actions:
1. Fix/disable the stale Flow ExApp probe path so it stops returning `401`.
2. Keep monitoring for reappearance of the same user-agent and endpoint.
3. If needed, explicitly bypass-list only this known self-probe IP in Nextcloud brute-force handling strategy (carefully scoped).
